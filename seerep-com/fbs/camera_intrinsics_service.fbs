
include "camera_intrinsics.fbs";
include "query.fbs";

include "server_response.fbs";

namespace seerep.fb;

rpc_service CameraIntrinsicsService {
  GetCameraIntrinsics(seerep.fb.Query):seerep.fb.cameraIntrinsics (streaming: "server");
  TransferCameraIntrinsics(seerep.fb.cameraIntrinsics):seerep.fb.ServerResponse (streaming: "client");
}
