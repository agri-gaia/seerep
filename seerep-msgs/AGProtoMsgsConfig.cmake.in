@PACKAGE_INIT@

set(AGProtoMsgs_INCLUDE_DIRS @PACKAGE_CMAKE_INSTALL_INCLUDEDIR@)

find_package(PkgConfig REQUIRED)
find_library(AGProtoMsgs_LIBRARY NAMES agprotomsgs)
set(AGProtoMsgs_LIBRARIES ${AGProtoMsgs_LIBRARY})

find_package(Protobuf REQUIRED)
list(APPEND AGProtoMsgs_INCLUDE_DIRS ${Protobuf_INCLUDE_DIR})
list(APPEND AGProtoMsgs_LIBRARIES ${Protobuf_LIBRARY})
