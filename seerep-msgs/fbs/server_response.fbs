
include "timestamp.fbs";

namespace seerep.fb.ServerResponse_;

enum TRANSMISSION_STATE : int {
  SUCCESS = 0,
  FAILURE = 1,
}

namespace seerep.fb;

table ServerResponse {
  seq:uint;
  stamp:seerep.fb.Timestamp;
  message:string;
  transmission_state:seerep.fb.ServerResponse_.TRANSMISSION_STATE;
}
